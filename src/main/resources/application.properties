spring.application.name=loginAPI

# ---------------------------------

# JWT secret key
jwt.secret=Xf9@kLm2!zR7#bNp8^VwQ1&uTyGhJ4dE

# ---------------------------------

# HTTPS request
server.port=8443
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-password=APiCr1ptoHTTP$
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=alia

# ---------------------------------

# Configurações de email para envio pela aplicação
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=criptocomofficial@gmail.com
spring.mail.password=Flamengo10$&@
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ---------------------------------

# Database connection
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/criptocom_teste?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Fla2001

# ---------------------------------

# Hikari - Testa conexões antes de usar
spring.datasource.hikari.validation-timeout=3000
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# Tempo máximo que a conexão pode ficar ociosa
spring.datasource.hikari.idle-timeout=60000

# Fecha conexões inativas antigas (por segurança)
spring.datasource.hikari.max-lifetime=1800000

# ---------------------------------

# Dialeto para MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Atualiza as tabelas automaticamente (cuidado em produção!)
spring.jpa.hibernate.ddl-auto=update

# Mostrar SQL no console
spring.jpa.show-sql=true

# ---------------------------------
